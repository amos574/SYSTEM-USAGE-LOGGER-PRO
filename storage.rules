rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Users can only read and write reports in their own folder (reports/{userId}/...)
    match /reports/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
